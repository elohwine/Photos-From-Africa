Perfect ✅ — below is a **ready-to-use Copilot prompt** that tells GitHub Copilot to generate a **full Java Spring Boot Stripe PaymentIntent integration**, complete with **example code snippets**, all aligned with **official Stripe documentation** (PaymentIntent API + Webhooks best practices).You can copy-paste this directly into **GitHub Copilot Chat**, and it will produce a full working implementation with comments, tests, and setup instructions.---### 🧠 **GitHub Copilot Prompt: Full Stripe PaymentIntent Flow (Spring Boot, Java)**```You are a senior Java Spring Boot engineer. Generate a complete, production-grade **Stripe PaymentIntent integration** in Java using **Spring Boot 3.x** and the official Stripe Java SDK (`com.stripe:stripe-java`). Follow Stripe's official docs for PaymentIntent creation, lifecycle, and Webhooks verification.Project name: `stripe-paymentintent-sample`✅ **Requirements:**- Java 17+- Maven project- Stripe official library (no third-party wrappers)- Read API key and webhook secret from environment variables  - `STRIPE_API_KEY`  - `STRIPE_WEBHOOK_SECRET`- Use standard Spring Boot structure- Include clean example code snippets (controller, service, config, webhook, etc.)- Include comments referencing relevant Stripe official docs---### 📁 **Project Structure:**```stripe-paymentintent-sample/├── pom.xml├── src/main/java/com/example/stripepaymentintent/│    ├── StripePaymentIntentApplication.java│    ├── config/StripeConfig.java│    ├── controller/PaymentController.java│    ├── controller/WebhookController.java│    ├── service/PaymentService.java│    ├── model/OrderStore.java│    └── dto/│         ├── CreatePaymentRequest.java│         └── CreatePaymentResponse.java├── src/main/resources/application.properties└── README.md````---### 🧩 **Example Snippet 1 — pom.xml**Use latest official `stripe-java` dependency:```xml<dependency>  <groupId>com.stripe</groupId>  <artifactId>stripe-java</artifactId>  <version>24.10.0</version></dependency><dependency>  <groupId>org.springframework.boot</groupId>  <artifactId>spring-boot-starter-web</artifactId></dependency>````---### 🧠 **Example Snippet 2 — StripeConfig.java**```javapackage com.example.stripepaymentintent.config;import com.stripe.Stripe;import org.springframework.context.annotation.Configuration;import jakarta.annotation.PostConstruct;@Configurationpublic class StripeConfig {    @PostConstruct    public void init() {        String apiKey = System.getenv("STRIPE_API_KEY");        if (apiKey == null) {            throw new IllegalStateException("STRIPE_API_KEY environment variable not set.");        }        Stripe.apiKey = apiKey;        System.out.println("Stripe initialized successfully.");    }}```---### 💳 **Example Snippet 3 — PaymentController.java**```javapackage com.example.stripepaymentintent.controller;import com.example.stripepaymentintent.dto.CreatePaymentRequest;import com.example.stripepaymentintent.dto.CreatePaymentResponse;import com.example.stripepaymentintent.service.PaymentService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.web.bind.annotation.*;@RestController@RequestMapping("/api/payments")public class PaymentController {    @Autowired    private PaymentService paymentService;    @PostMapping("/create-payment-intent")    public CreatePaymentResponse createPaymentIntent(@RequestBody CreatePaymentRequest request,                                                     @RequestHeader(value = "Idempotency-Key", required = false) String idemKey) throws Exception {        return paymentService.createPaymentIntent(request, idemKey);    }}```---### ⚙️ **Example Snippet 4 — PaymentService.java**```javapackage com.example.stripepaymentintent.service;import com.example.stripepaymentintent.dto.CreatePaymentRequest;import com.example.stripepaymentintent.dto.CreatePaymentResponse;import com.example.stripepaymentintent.model.OrderStore;import com.stripe.model.PaymentIntent;import com.stripe.param.PaymentIntentCreateParams;import com.stripe.net.RequestOptions;import org.springframework.stereotype.Service;@Servicepublic class PaymentService {    public CreatePaymentResponse createPaymentIntent(CreatePaymentRequest request, String idemKey) throws Exception {        PaymentIntentCreateParams params = PaymentIntentCreateParams.builder()            .setAmount(request.getAmount())  // amount in cents            .setCurrency(request.getCurrency())            .addPaymentMethodType("card")            .putMetadata("orderId", request.getOrderId())            .setDescription("Payment for order " + request.getOrderId())            .build();        RequestOptions options = (idemKey != null) ?             RequestOptions.builder().setIdempotencyKey(idemKey).build() : RequestOptions.getDefault();        PaymentIntent intent = PaymentIntent.create(params, options);        // Simulate storing the order        OrderStore.saveOrder(request.getOrderId(), intent.getId(), "CREATED");        return new CreatePaymentResponse(intent.getId(), intent.getClientSecret());    }}```---### 🪝 **Example Snippet 5 — WebhookController.java**```javapackage com.example.stripepaymentintent.controller;import com.example.stripepaymentintent.model.OrderStore;import com.stripe.model.Event;import com.stripe.model.PaymentIntent;import com.stripe.net.Webhook;import org.springframework.web.bind.annotation.*;import org.springframework.http.ResponseEntity;@RestController@RequestMapping("/api/webhooks")public class WebhookController {    private final String webhookSecret = System.getenv("STRIPE_WEBHOOK_SECRET");    @PostMapping("/stripe")    public ResponseEntity<String> handleWebhook(@RequestHeader("Stripe-Signature") String sigHeader,                                                @RequestBody String payload) {        try {            Event event = Webhook.constructEvent(payload, sigHeader, webhookSecret);            switch (event.getType()) {                case "payment_intent.succeeded":                    PaymentIntent succeeded = (PaymentIntent) event.getDataObjectDeserializer().getObject().get();                    OrderStore.updateStatus(succeeded.getId(), "SUCCEEDED");                    break;                case "payment_intent.payment_failed":                    PaymentIntent failed = (PaymentIntent) event.getDataObjectDeserializer().getObject().get();                    OrderStore.updateStatus(failed.getId(), "FAILED");                    break;                default:                    System.out.println("Unhandled event type: " + event.getType());            }        } catch (Exception e) {            return ResponseEntity.badRequest().body("⚠️ Webhook error: " + e.getMessage());        }        return ResponseEntity.ok("✅ Received");    }}```---### 🧱 **Example Snippet 6 — CreatePaymentRequest.java**```javapackage com.example.stripepaymentintent.dto;public class CreatePaymentRequest {    private Long amount;    private String currency;    private String orderId;    // getters and setters}```---### 🧾 **Example Snippet 7 — CreatePaymentResponse.java**```javapackage com.example.stripepaymentintent.dto;public class CreatePaymentResponse {    private String paymentIntentId;    private String clientSecret;    public CreatePaymentResponse(String id, String secret) {        this.paymentIntentId = id;        this.clientSecret = secret;    }    // getters}```---### 🗂️ **Example Snippet 8 — OrderStore.java**```javapackage com.example.stripepaymentintent.model;import java.util.concurrent.ConcurrentHashMap;public class OrderStore {    private static final ConcurrentHashMap<String, String> store = new ConcurrentHashMap<>();    public static void saveOrder(String orderId, String paymentIntentId, String status) {        store.put(orderId, status + "|" + paymentIntentId);    }    public static void updateStatus(String paymentIntentId, String status) {        store.replace(paymentIntentId, status);    }    public static void printOrders() {        store.forEach((k, v) -> System.out.println(k + " -> " + v));    }}```---### 📘 **README.md (example content)**````markdown# Stripe PaymentIntent Spring Boot Example## Setup```bashexport STRIPE_API_KEY=sk_test_your_secret_keyexport STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secretmvn spring-boot:run````## Test1. Run the server (`mvn spring-boot:run`).2. Create a PaymentIntent:   ```bash   curl -X POST http://localhost:8080/api/payments/create-payment-intent \   -H "Content-Type: application/json" \   -d '{"amount":1099, "currency":"usd", "orderId":"ORD123"}'   ```3. Listen for webhooks:   ```bash   stripe listen --forward-to localhost:8080/api/webhooks/stripe   ```4. Test with Stripe test cards:   * Visa: `4242 4242 4242 4242`   * AMEX: `3782 822463 10005`   * 3D Secure test: `4000 0027 6000 3184````---### ✅ Official Stripe docs references:- PaymentIntent API: https://stripe.com/docs/api/payment_intents- PaymentIntents lifecycle: https://stripe.com/docs/payments/payment-intents- Webhooks verification: https://stripe.com/docs/webhooks- Test cards: https://stripe.com/docs/testing---**Instructions for Copilot:**Generate the full implementation including the snippets above, making sure it compiles as a working Maven project, with clear comments and references to official Stripe docs.```---