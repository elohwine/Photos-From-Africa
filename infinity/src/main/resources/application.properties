# JPA/Hibernate (dev-friendly defaults; adjust for prod)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Database Configuration (Render PostgreSQL)
# IMPORTANT: Do not embed username/password in the JDBC URL. Provide them via separate properties.
spring.datasource.url=jdbc:postgresql://dpg-d45icq8dl3ps738egthg-a.frankfurt-postgres.render.com:5432/cryptoplatform?sslmode=require
spring.datasource.username=cryptoplatform_user
spring.datasource.password=P4auoXSg8XbtPBF5tmkLeLzR2ZAkBFrO

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:}
spring.mail.password=${MAIL_PASS:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.devtools.livereload.enabled=true
spring.devtools.restart.exclude=static/**

# Stripe configuration via environment variables
# export STRIPE_API_KEY=sk_test_xxx
# export STRIPE_WEBHOOK_SECRET=whsec_xxx
